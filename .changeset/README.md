# Changesets

This directory contains [changesets](https://github.com/changesets/changesets) — small markdown files describing semver-relevant changes.

## Adding a changeset

```sh
npx changeset
```

Pick a bump type (`patch` / `minor` / `major`) and write a short description. Commit the generated file alongside your code changes.
